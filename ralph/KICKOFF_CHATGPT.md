# Kick-off prompt — ChatGPT Cowork / Codex

Paste everything below the line into a new ChatGPT Cowork session with the `EVEN G2 HUD` folder (or a local clone of it) connected. It is written for GPT running as the driver of the loop; the same text works for Claude Code if you swap "codex" for "claude" in the runner line.

---

You are picking up the **G2 Race Spotter** project: a spotter's phone sends lane calls (▲ ● ▼), a "car behind" gap, and short messages through a Cloudflare Durable Object relay to a driver's phone, whose Even Realities Hub app renders them on G2 smart glasses. All context is in the repo; nothing lives in chat history. Work in this order and do not skip steps.

**1. Orient (read, don't act yet).** Study `AGENTS.md` (operational guide), `specs/000-constitution.md` (invariants and process rules), `ralph/README.md` (how the loop works), then skim `specs/010`–`070`, `IMPLEMENTATION_PLAN.md`, and `ralph/PROGRESS.md`. Treat `docs/BUILD_PLAN.md` and `docs/RESEARCH_NOTES.md` as rationale; specs win on conflict. Don't assume anything is unimplemented — check the tree.

**2. Set up the working copy.** If this folder is the OneDrive-synced one, first clone or copy it to a local path outside OneDrive and work there (the loop and `node_modules` do not belong in a synced folder). Initialise git if there is no `.git`, commit everything as `chore: import planning package`. Confirm Node 22 and git are available. Run `node scripts/sync-agents.mjs --check`; if stale, run it without `--check` and commit.

**3. Pre-flight the model routing.** Open `ralph/models.env`. The Codex model ids (`CODEX_WORKER_MODEL`, `CODEX_REVIEWER_MODEL`, `CODEX_PLANNER_MODEL`) are placeholders. Replace them with the real ids available on this account for: worker = GPT 5.6 Terra, reviewer = GPT Sol 5.6, planner = GPT Sol 5.6 (verify with the CLI or the model list; do not guess). Keep the efforts as set (worker high, reviewer high, planner xhigh; if a model rejects xhigh the loop retries at high). Then work through the "Verify before first run" list in `ralph/README.md` against `codex exec --help` and fix `ralph/loop.sh` only where a flag genuinely differs on this install — keep changes minimal and note each one in `ralph/PROGRESS.md`. Rerun `node scripts/sync-agents.mjs` and commit as `chore: model routing for codex`.

**4. Run the loop once.** `RUNNER=codex ralph/loop.sh once`. Watch `ralph/logs/`. If the `codex` CLI cannot be driven from here (no shell, no CLI), fall back to running the three stages yourself in this session, in three clearly separated turns, each time reading the stage prompt fresh and adopting the named role: build (`ralph/PROMPT_build.md`, role = the `owner:` of the first unchecked task in `IMPLEMENTATION_PLAN.md`), review (`ralph/PROMPT_review.md`, role `protocol-keeper` if the diff touched `packages/protocol` or `services/relay/src`, else `hud-qa`), replan (`ralph/PROMPT_replan.md`, role `plan-updater`). Obey each prompt's §999 guardrails exactly — one task, one commit, tests green before commit, reviewer writes only `ralph/last-review.md`, planner never edits application code, only a human weakens a spec.

**5. Report back to me** with: the commit list from this session, the verdict in `ralph/last-review.md`, the `## Next` and `## Needs human` sections of `IMPLEMENTATION_PLAN.md`, any change you made to `loop.sh` or `models.env`, and any spec you marked `DISPUTED`. Then stop and wait; do not start a second iteration until I say "continue" (after that, `RUNNER=codex ralph/loop.sh` may run unattended until `ralph/DONE`, `ralph/STOP`, or `MAX_ITERATIONS`).

**Rules that override everything else:** never assign yourself an `[HW]` task (glasses, phones, Cloudflare login — those are mine; list them under *Needs human*); never redefine a wire type outside `packages/protocol`; never let the driver see stale data as live; never commit secrets; never edit `.claude/`, `.codex/` or `.claude/skills` by hand — edit `agents/roles/` or `.agents/skills/` and re-run the sync script.
