# Reviews — 2026-09-04 round 2 (T009, T017)

Integrated on master as `b28cc2c` (T009, cherry-picked from L015 `697ad4c`) and `c9fb8b6` (T017, from L016 `46afe01`) + `8bf5531` (auditor: freeze NON_CLOSE_DETAIL). Reviewer: protocol-keeper (Opus 5 @ high), fresh context per range. Auditor: Fable 5.1 — read both source diffs, confirmed the reviewers' file:line claims, re-ran every gate on the integration branch.

## L015 — T009

```
task: T009
reviewer: protocol-keeper
verdict: approve with nits
findings:
  - [nit] services/relay/src/race-room.ts:79,108 — `persistAndBroadcast` and `hasReadyPeer` filter on `ready === true` only, not `rejected !== true`. Eviction is the first path that produces a `ready:true, rejected:true` attachment, so these filters now rely on the runtime removing a server-closed socket from `getWebSockets()` rather than on the `rejected` flag they were given. — scenario: today benign — I probed it at live `wrangler dev` (driver A evicted, driver B then closes) and the spotter correctly received `driverOnline:false`, so the evicted socket is genuinely gone from `getWebSockets()`. It only bites if a future path (T012's alarm reaper, or a rehydrated-but-not-yet-reaped socket) leaves a rejected socket in the set: it would then be counted as a live driver and be sent state frames after its `error{role_taken}`. — fix: add `&& attachment.rejected !== true` to both predicates (T012 already lists "exclude sockets already closed/rejected" in scope; fold it there rather than reopening T009).
  - [nit] services/relay/test/eviction.test.ts:113 — `expectNoMessage(first, 300)` runs after the test has already awaited `evictedClose` (4409), so `ws` cannot emit a message on that socket regardless of relay behaviour; the assertion is close to tautological. The load-bearing half (the new driver does receive the lane broadcast) is asserted properly, so this is not a §16 placeholder. — fix: none required; if strengthened later, arm the no-message watch before the eviction rather than after the close resolves.
verified:
  - Range/scope: `1801ef2..697ad4c` is exactly one worker commit (`1801ef2` is the planner's lease commit); `git show --stat` = only `services/relay/src/race-room.ts` (+28) and `services/relay/test/eviction.test.ts` (+176). Inside the L015 write scope; `live-worker.ts` untouched. (Note: `ralph/last-build.md` records base `d633a5d`, one commit below the leased base — same content, no finding.)
  - Placement (race-room.ts:271-296): eviction sits in the `!ready` hello branch after the stored-PIN compare (:253-269) and before `serializeAttachment({...ready:true})` (:298). Every earlier rejection returns first, so a wrong-PIN (:261), bad-4-digit-token (:243), version-mismatch (:220) or role/name-mismatch (:235) hello can never evict. Probed live: a `hello{v:99}` joiner got `error{version}` and the sitting driver kept receiving broadcasts.
  - Victim selection (:272-283): `this.ctx.getWebSockets('driver')` filtered on `ready === true && rejected !== true && existing !== socket` — all stale drivers, never the joiner, no name comparison, so a driver reconnecting on a new socket evicts its own stale one (last writer wins, never locked out).
  - Order (:287-293) via existing `rejectSocket`: `rejected:true` attachment written, then `error{role_taken}`, then `close(4409)`. Matches the skill.
  - Reconciliation: no extra `reduce`/`scheduleAlarm` added; the joiner is marked ready before the single `reconcilePeers`, so `driverOnline` stays `true` across the swap. Probed live with a spotter connected through the swap: zero frames leaked to it and `seq` stayed at 2 (no spurious `driverOnline=false`, no double bump). Alarm remains self-arming — armed only on the ready path (:301), never on data frames.
  - Hibernation: victims are found via `getWebSockets` + `deserializeAttachment` (`this.attachment`), no in-memory socket map, so eviction works on rehydrated sockets. `isSocketAttachment` still passes (`rejected` is optional boolean).
  - AC-5 boundary: `eviction.test.ts` runs against real `wrangler dev` via `startWorker()`; message/close promises are armed before each triggering send; `seq` asserted as `expect.any(Number)` / `toBeGreaterThan(0)`, never exact. Covers correct-PIN eviction (`error{role_taken}` + 4409), the new driver's first post-hello frame being `state`, wrong-PIN joiner rejected `auth`/4401 with the first driver untouched and still receiving broadcasts, and a post-eviction spotter lane change reaching only the new driver. No placeholder assertions (§16 clean).
  - Invariants §1-§8: no client/HUD code touched; constants (`CLOSE_CODE_DRIVER_EVICTED`) imported from `@g2-race-spotter/protocol`, nothing re-typed (§4); strict TS, no `any`, no secrets, no `localStorage`.
  - Gates run by me in this worktree: `npm run typecheck` clean; `npm test` 17 files / 86 passed; `npx eslint` on both changed files exit 0; `npx prettier --check` on both files clean. Root `npm run lint` failure is the known pre-existing `services/relay/test/auth.test.ts` formatting issue, not this commit.
range: 1801ef2..697ad4c
```

## L016 — T017

```
task: T017
reviewer: protocol-keeper
verdict: approve with nits
findings:
  - [nit] packages/protocol/src/client.ts:50 — `NON_CLOSE_DETAIL` is a shared mutable object handed to every `connecting`/`open` notification — scenario: a UI writes `detail.terminal = true` on the object it receives during `open`; every later non-close notification for every client in the process then reports `terminal: true` — fix: `Object.freeze` it, or build `{code: undefined, terminal: false}` per call.
  - [nit] packages/protocol/src/client.ts:343 — a throwing `onError` subscriber escapes `handleMessage` — scenario: spotter UI's error handler throws while rendering the PIN prompt; a second `onError` subscriber never runs and the exception surfaces in the socket's message listener. Same pre-existing shape as `onState`/`setConnectionState`, so not a regression — fix (whole-file, later task): wrap each `listener(...)` in try/catch.
  - [nit] packages/protocol/test/client-errors.test.ts — the new `{code, terminal}` detail is only asserted on `addEventListener` fakes. `client.test.ts:446` still covers the Node numeric-close path (the T006a regression) but only for "no reconnect timer"; nothing asserts `detail.code === 4409 / terminal === true` when the close arrives as a bare number via `on()` — fix: one case in the existing 446 loop with a two-arg `onConnection`.
  - [nit] packages/protocol/test/client-errors.test.ts:145 — no test pins the ordering the relay actually produces (`error` frame, then terminal close): that `onError` fires before the `closed` callback. Correct by construction (separate events, dispatched synchronously in `handleMessage`), but it is the exact 060 R3 sequence and is unasserted.
  - [nit] packages/protocol/src/client.ts:210 — `connect()` now tears down a socket that is open, hello sent, and awaiting replay. This is what the plan asked for and it is not a thrash loop (`connect()` is externally driven; `reconnectAttempt` is untouched, and a retired driver socket's late 4409 is dropped by the identity guard at :365), but on a slow link a 060 foreground/visibilitychange handler that calls `connect()` unconditionally will discard healthy handshakes — fix: none here; note in the 030/060 consumer task that foreground `connect()` should be debounced.
  - [nit] packages/protocol/test/client-errors.test.ts:13 — FakeSocket/FakeWebSocket/FakeTimers duplicated from client.test.ts (worker flagged; write scope forbade a shared helper). Follow-up: `test/support/fakes.ts`.
verified:
  - Criterion: all three verify clauses of T017 are exercised for real — error frame -> `onError` (:145, :160); 4409 -> `closed` with `{code:4409, terminal:true}`, queue emptied and proven by a fresh same-URL session sending only `hello` (:180), `timers.timeouts.size === 0`; `connect()` on a never-opening CONNECTING zombie -> second socket created and the zombie's late `open`/`message` ignored (`lastSeen` stays 0, then 1 from the fresh socket) (:263). Non-terminal 1006 and 4408 assert `terminal:false` + exactly one reconnect timer. `now`/`random` pinned. No placeholders (§16).
  - Protocol fidelity: `terminal` comes from the existing `isTerminalClose`, which compares the imported `CLOSE_CODE_*` constants; tests import the constants too (only 1006, an RFC code with no protocol constant, is literal). `error` frames go through `isErrorMessage`; `lastFrameAt` is still stamped for error/unknown frames and cleared on terminal close per the 030 2026-09-04 decision. `lastSeen`/`replayed` reset on open unchanged; no intent re-send.
  - Stale-socket safety: terminal close on a retired socket returns at the `this.socket !== socket` guard (:365) before `clearPending()`, so it cannot wipe the current session's queue. `connect()` clears the reconnect timer (:216) before `openSocket()`, and `scheduleReconnect` self-guards, so a same-URL `connect()` during a pending reconnect does not double-schedule. `connect()` also clears the ping timer before retiring, so an open-but-unreplayed zombie leaks no interval.
  - Scope: exactly one commit, exactly the three leased files. `client.test.ts` byte-identical.
  - Gates run in /home/claude/wt/L016: `npm run typecheck` clean; `npx vitest run packages/protocol` 49/49 (5 files); `npm test` 90/90 (17 files); `npx eslint` and `npx prettier --check` on the three files both clean. Root `npm run lint` not used (known pre-existing auth.test.ts prettier failure at this base).
range: 1801ef2..46afe01
```

## Auditor disposition

- T009 nit 1 (`persistAndBroadcast`/`hasReadyPeer` do not filter `rejected`): folded into T012 as an explicit trap. T009 nit 2 (post-close no-message watch is tautological): accepted as-is; noted for T014's driver-evict scenario.
- T017 nit 1 (shared mutable `NON_CLOSE_DETAIL`): fixed by the auditor in `8bf5531` (`Object.freeze`, `Readonly<>`); typecheck/prettier/49 protocol tests green. Nit 2 (throwing listener): pre-existing shape, filed as T018. Nit 3/4 (numeric-close detail and error-before-closed ordering unasserted): filed as T018 verifier additions. Nit 5 (`connect()` tears down an open-awaiting-replay socket): accepted per the task; 060 consumer tasks must debounce foreground `connect()` — recorded in the plan Notes. Nit 6 (duplicated fakes): T018 may extract `packages/protocol/test/support/fakes.ts`.
- Gates on master `8bf5531`: typecheck; 92/92 (18 files, live wrangler dev); lint 0 errors / 2 generated-d.ts warnings; prettier clean; pins exact; sync up to date; `wrangler types --check` up to date; isolated-assets `deploy --dry-run` ok.
- Process note: root `npm run lint` had been failing on a prettier line in `auth.test.ts` since `4181e34`; the Windows gate run filtered the prettier output and reported lint green. Fixed in `9fd612b`; lesson recorded in PROGRESS.
