# Reviews — 2026-09-25 round 7 (design round 4: T056 spotter/protocol, T057 glasses)

T056 (L022 `4c7c4fa..fec29ff` → master `4aabe89`,`3c9a86c`,`7c6a0e2`): protocol-keeper + hud-qa (Opus 5) **approve with nits**. T057 (L023 `707f740..a8037ed` → master `325d466`,`6da4a77`,`ff4c4ab` + auditor T057b): the worker STOPPED before coding because the SDK caps a page at 4 image containers (six were planned) — re-scoped to four edge strips; hud-qa **block** (skip-unchanged compared against in-flight bytes → a failed send could never be retried) → fixed → **approve with nits**. Auditor: integrated, 323/323 + every gate exit 0; applied the leftover-dirty-flag nit (T057b).

## T056
```
task: T056
reviewer: protocol-keeper + hud-qa
verdict: approve with nits
findings:
  - [nit] packages/protocol/src/index.ts:230-236,360 — `isState` enforces `PRESETS_MAX` and uniqueness on a field the glasses ignore. — scenario: a later "additive" relay raises PRESETS_MAX to 20 → a room with 13 presets fails `isState` on older glasses/spotters → every state frame is dropped → driver NO LINK. — fix: have the guard check only the `string[]` shape, or record in the skill that changing PRESETS_MAX requires a version bump.
  - [nit] apps/spotter/src/main.ts:466-467,343-350 — a drag's `base` triple is captured at pointerdown and sent whole at pointerup. — scenario: LEFT=3 was called at t=0; the spotter holds MIDDLE from 5.5 s to 7 s; the relay stale-clears at 6 s; pointerup sends [3,n,0] and LEFT=3 comes back on the glasses (the same resurrection T055a fixed for reconnects, now with a longer window). — fix: at pointerup, build the triple from the current `selectedCars` with only `drag.row` replaced.
  - [nit] apps/spotter/src/main.ts:248-261 — offline (`state === null`) the cap check sees [] and the draft is cleared, even though the queued `preset add` will be a no-op at 12. — scenario: the typed text is lost silently. — fix: keep the draft until the replay shows the preset.
  - [nit] test coverage — the main.ts wiring for `say`/`preset-send`/`preset-remove`/`save` (main.ts:433-446) is never driven through main.ts; view.test only asserts `data-act` attributes. — fix: add a main-level click test that asserts the frames sent (`msg`, `preset add/remove`).
  - [nit] FRAME_MAX_BYTES — nothing enforces it anywhere (grep: defined only at index.ts:62; no size check in the relay, RoomClient or either app). The new skill sentence ("client frames only, nothing inbound-checks server frames") matches the code, but the skill's table row "relay" and agents/roles/protocol-keeper.md:26 ("enforced in the relay") are stale. — planner.
  - [nit] specs/040: R2 (stacked lanes), R3 ("rows… There is no slider"), R4 (5 localStorage chips), R5 (full-width banner) and Scope ("recent-message chips", "reconnect banner") now contradict the code; the Decision says they are superseded, so the planner should fold that into the requirement text. AC-3 (the pill) and AC-8 (top-half and chip checks) were edited beyond the plan's "AC-1/AC-2 rewritten". Both edits clarify or strengthen, so there is no weakening, but the planner should ratify them.
  - [nit] out of lease, still 12 h / old layout: agents/roles/protocol-keeper.md:25, relay-backend-dev.md:29, spotter-pwa-dev.md:23-26, docs/BUILD_PLAN.md:89,101,103,110, cloudflare-relay-deploy SKILL.md:64; L022 is not in the plan's Active leases.
  - [nit] apps/spotter/src/join.ts:11 — the alphabet keeps O/0 and I/1, which the driver can misread when copying the code. It follows the spec (`[A-Z0-9]`); raise with Maxx. The code itself is legible: 56 px mono on Join, min(20vw,120px) in the overlay, 15 px in the header chip.
  - [note] Clear button removal is acceptable: plan (d) specifies [input][Send][Save] and the 040 Decision records it; `clear` stays in the protocol but the UI never sends it, and the glasses' auto-ack plus the 6 s stale clear retire a message. Confirm with Maxx, as the worker flagged.
verified:
  - Protocol: `presets?` optional, and every reader uses `?? []` (reduce.ts:216, model.ts roomPresets, relay loadState:79). `isState` rejects a wrong-typed, >12, duplicate or untrimmed list. `preset` is spotter-only (a driver frame is a no-op in the reducer and live-tested with no seq bump), dedupes, trims, caps at 80/12, never stamps calledAt; `stale` keeps presets, `isHudEmpty` ignores them, `expire` drops them; no-op edits do not bump seq. RoomClient queues `preset` in order with msg/clear (client.test). No inbound size check exists on either client or the relay.
  - 24 h TTL: the constant is asserted (guards.test:202); alarm.ts/race-room.ts use ROOM_TTL_MS. The live expiry test rewrites the stored SQLite `updatedAt` to TTL+1 s in the past, then an un-helloed probe close re-points the alarm into the past. It asserts createdAt/alarm null and that a rejoin with PIN 9999 (the room was created with 1234) gets a `state` with presets [], which proves deleteAll removed both the presets and the PIN.
  - Spotter: header icons, pill, driver dot, ack, latency and overlay are as specified; lanes ▼▬▲ left to right; three bottom-up sliders (tap n, tap the lit top → 0, drag sends once on pointerup, pointercancel sends nothing, the following click is swallowed; main-rebase.test); built-ins match the texts exactly; chips come from state.presets only; recent-messages.ts/test are deleted and `recentMsgs` is removed on load. Join: 6-char crypto code without modulo bias, no Math.random, 4-digit PIN required, one collision retry through the real main.ts (join.test), classic form, 24 h seenAt resume. Top-level vdom child counts are constant; the only variable list is the chips, inside its own container.
  - layout.test (Chromium): 390x664, 360x640 and 740x360, relay down and live with 3 presets: document = console = viewport height (no scroll), buttons min 44 px (21/27), segments ≥ 44, lanes ≥ 64, sliders end y=276 (43 % of 640, 42 % of 664), chips fit (150/126/142). Bundle: main JS 7694 B gz + sw 815 B.
  - Gates (exit 0 each): typecheck; npm test 39 files / 300 tests; lint (0 errors, 2 pre-existing warnings); check:pins; sync:agents:check (.agents ≡ .claude); spotter build; wrangler types --check. Worktree clean; the diff stays inside the lease (protocol, relay, spotter, the two skills, 020/040).
range: 4c7c4fa..fec29ff (f00072c, 71d1269, fec29ff: the coordinator-defined three-commit lease)
```

## T057
```
task: T057 (+ T057a fix)
reviewer: hud-qa
verdict: approve with nits
findings:
  - resolved (was block) apps/glasses/src/render/queue.ts:256 — pushImages now compares only with `lastSent`. On success (:439-445) a container whose newest `desired` differs from what just landed is re-marked dirty. Traced each case:
    - A push back to the OLD bytes during a successful flight: desired == old lastSent, so the push clears dirty. On success, lastSent = new ≠ desired, so the container is re-marked and re-sent. Nothing is lost (existing revert test).
    - A same-bytes push during a failing flight: desired ≠ old lastSent, so it stays dirty and is re-sent once (new test, queue-image.test.ts:358).
    - A same-bytes push during a successful flight: the stale dirty flag is dropped at send time (:423-428) with no bridge call (new test :387).
    - A failing flight gets no re-mark. That leaves the no-push-no-retry rule, so there is no loop: a scratch run with `imageSizeInvalid` every time made exactly 2 calls for a lane change and 0 afterwards.
    - A re-mark needs a push that changed `desired` during a flight, so it is bounded by pushes.
  - [nit] queue.ts:439-445 — on success with desired == bytes, the dirty flag set by a same-bytes push survives. It costs no call, but for stripBL/BR it opens an empty bottom flush that resets `lastBottomAt`. That can push the next real car change back by up to 250 ms, still inside the debounce bound. — fix: `else this.dirty.delete(containerID)`.
  - [nit] still open for the planner/Maxx:
    - The status rect 40×28 clips CONNECTING…, ROOM ? and the terminal strings.
    - 050 Scope/R1 and constitution §3 still say "one image container".
    - Failures are counted per send, so 3 inside one fan-out → text mode ([HW] T109/T110).
    - [SIM] AC-5b has not been re-run for the 4-image page.
  - fixed nits from the first review: R2 is reworded to "one flush (≤ 2 sends)"; AC-4 now says middle "≤ 2"; the seam test covers every golden plus dim; the skill documents the seam tear and the per-send fallback count.
verified: `npm run typecheck` 0; `npm test` 0 (38 files / 289 tests). Earlier on 45dd7c5: lint 0, sync:agents:check 0, build -w apps/glasses 0. The fix commit a8037ed touches only apps/glasses, spec 050 and the g2-hud-display skill + mirror.
range: 707f740..a8037ed (re-review 45dd7c5..a8037ed)
```

## Open for Maxx / planner
- Status strip 40×28 clips `CONNECTING…` / `ROOM ?` / terminal strings (Maxx: shorter codes, or show them in the message container).
- Room-code alphabet keeps O/0 and I/1 (spec says [A-Z0-9]); drop them?
- `isState` enforces PRESETS_MAX — raising it later needs a version bump (record in skill).
- Drag held across a stale clear can resurrect other rows (build the triple at pointerup from current state).
- Offline Save clears the draft even when the cap will reject it.
- T054 housekeeping: 040 R2–R5/Scope, 050 Scope/R1, constitution §3, AGENTS.md, role files, BUILD_PLAN, cloudflare-relay-deploy (12 h → 24 h), hud-e2e-testing (4-container page), FRAME_MAX_BYTES wording.
- [SIM] AC-5b not re-run for the 4-image page; [HW] T109/T110 must measure per-call cost (failures now counted per send).
