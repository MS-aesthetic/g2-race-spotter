# Reviews — 2026-09-04 rounds 4–5 (design rounds 1–2: T052, T053)

T052 (L019 `48384cb..b626ed0` → master `56d979b`,`510bd90`,`b07aad5`): protocol-keeper + hud-qa (Opus 5), **approve with nits**. T053 (L020 `7421717..0675afd` → master `c0e5d18`,`b6d94cc`,`9276828`,`50678f2`): hud-qa (Opus 5) **block** (dropped auto-ack unrecoverable when the socket is down at the 5 s mark — reproduced) → fixed by the worker in `0675afd` with a retry-on-replay and a socket-down test → re-review **approve with nits**. Auditor: Fable 5.1 — integrated, full gates on master (258/258, exit codes), redeployed relay + spotter, verified live in the simulator: message auto-acked 5.1 s after display; spotter console fits 390×664 / 360×640 / 740×360 in real Chromium.

## T052
```
(see agent transcript; worktree removed)
```

## T053 (final)
```
task: T053 (re-review of the fix commit 0675afd)
reviewer: hud-qa
verdict: approve with nits
range: 7421717..0675afd — this pass reviewed e66ad2b..0675afd only

findings:
- Block fixed, and for the right reason. driver.ts:120-168 holds the auto-acked id as `pendingAckMsgId`; `retryPendingAck` re-sends on
  every `state` still showing it unacked, and clears on `ackedAt !== null`, on a different `msg.id` and on `msg: null`; `stop()` clears
  the new timer. The `setTimeout(…, 0)` deferral is necessary, not cosmetic — client.ts:364-366 sets `replayed` *after* the state-listener
  loop and send() at :260-266 explicitly does not queue an `ack`, so an inline retry on the first frame after a reconnect is dropped.
- ack.test.ts:214-251 exercises exactly the branch I broke: `socket.close(1006)` at t=3 s, the window closes at t=5 s with the socket dead
  (asserts the text cleared *and* zero acks), a genuine `FakeWebSocket` replacement replays m1 unacked, `advance(0)` runs the deferred
  send, and it asserts exactly `[{t:'ack',msgId:'m1'}]`; it then feeds `ackedAt: 99` and a later `msg:null` frame and asserts the count
  stays 1 — the retry stops. That is the inverse of the failure I reproduced last pass, so it is real backpressure, not decoration.
- `nextGap(current, tapped)` (intents.ts:35-47) compares against `currentGap` (exact, un-snapped), so with the room at 40 a tap on the lit
  `50` is now sent; `selectedGap` still snaps for the highlight only. intents.test.ts covers 40→50 by name, the exact-match no-op, the snap
  table (NaN/-10/400 included), and restores the `nextSide` coverage that went out with slider.test.ts.
- layout.test.ts:46-140 — `CHROME_PATH` → `chromium.executablePath()` → directory scan, throws under `CI` rather than skipping, `afterAll`
  resolves when `server` is undefined. Plus `minmax(0, 1fr)` at styles.css:255, stale slider prose gone, and the g2-hud-display sentence I flagged as false now describes the tap/auto split accurately.
- [nit] driver.ts:143-166 — the retry is uncapped: while a message stays unacked every `state` arms another send, so a relay that keeps
  dropping the ack (rate limiter) costs one extra `ack` per frame. Self-limiting (one in flight, ends with the message); a 2-3 attempt cap
  would be tidier. And main.ts:165-175 is still unreachable from a test (import-unsafe top-level `getElementById`) — the *decision* is unit-
  tested now, the wiring is not; later task: `export function start(root)`.
- [nit] unchanged → planner, outside this lease: `race-relay-protocol` SKILL.md:78/81, `hud-e2e-testing` SKILL.md:17, `agents/roles/spotter-pwa-dev.md`:3/18/24/33 and `docs/BUILD_PLAN.md` §101-102 still say slider/throttle; `scripts/check-pins.mjs:5` should gain `playwright-core` (pinned at 1.62.1, unenforced).

verified (this worktree, exit codes read): `npm run typecheck` 0; `npm test` 0 — 35 files / 258 tests, no `eviction.test.ts` flake.
layout.test.ts re-run verbose: 390×664 664<=664, 360×640 640<=640, 740×360 360<=360, 16 buttons min 44 px, lanes min 86.3 px; and
`CI=1 PLAYWRIGHT_BROWSERS_PATH=/nonexistent` fails the file instead of skipping it. Scope clean — only leased paths plus the generated `.claude/skills` mirror.
```

## Open nits for the planner
- Docs drift: 030 R3/AC-4, constitution §1, BUILD_PLAN §5 still describe the old NO LINK / LINK OK strip; race-relay-protocol SKILL :78/:81, hud-e2e-testing SKILL :17, agents/roles/spotter-pwa-dev.md, BUILD_PLAN §101-102 still say slider/throttle. `scripts/check-pins.mjs` should cover `playwright-core`.
- Glasses: leading-space `  S` blink phase unverified on the baked font ([HW]); arrow margin 4 px and 1 px checker/level-4 outlines unverified on hardware; ack retry uncapped (self-limiting).
- Spotter: `main.ts` wiring untestable until `export function start(root)`; BUMPER label tight at 390 px.
- Relay live tests flaky under full-suite parallelism (eviction, roundtrip side call) — wait for matching frames everywhere.
