# Reviews — 2026-09-25 round 6 (design round 3: T055)

Lease L021 `e544cd2..3bc4e2e` → master `4f1c746`..`b1dbb78` + auditor `eb6b110` (carry a discarded v1 room's updatedAt). Reviewer: protocol-keeper + hud-qa (Opus 5). Two blocks, both real and both reproduced: (1) the 6 s stale window was keyed on `updatedAt`, which the 5 s auto-ack bumps → cleared at ~11 s; fixed with `State.calledAt` (spotter-originated changes only). (2) the v2 relay trusted v1-shaped stored rooms → a redeploy mid-session bricked the room until TTL; fixed by validating stored state with `isState`. Final: approve with nits.

```
task: T055 (final, lease e544cd2..3bc4e2e; this pass reviewed 8aec37a..3bc4e2e, T055b)
reviewer: protocol-keeper + hud-qa
verdict: approve with nits
findings:
  - [nit] services/relay/src/race-room.ts:66-76 — a discarded v1 room restarts from `createInitialState()`, so `updatedAt` is 0. If the old v1 alarm fires before anyone rejoins, `alarm()` sees 0 sockets and `now >= 0 + ROOM_TTL_MS`, so it expires the room at once and `deleteAll()` drops its PIN and createdAt. The next joiner re-creates the room with their own token. Only a room nobody touched since the deploy is affected, and it's no worse than a new room, so acceptable. Carrying the stored `updatedAt` over (when it is a finite number) would keep the 12 h TTL.
  - [nit] (from round 1, still open, out of scope) AGENTS.md:3,47, constitution §3, the hud-e2e-testing skill, and the spotter-pwa-dev/protocol-keeper role files still describe `gap`. Planner follow-up.
Resolved:
  - Round 1 block (clear keyed on `updatedAt`): fixed in 8aec37a. `State.calledAt` is stamped by spotter calls only, set to 0 by stale/expire, and left alone by ack/peer; the relay's alarm keys on it. The live QA35 test clears at 6002 ms despite a driver ack at 5 s.
  - Round 2 block (v1 stored state): `loadState` keeps storage only if `isState` accepts it, otherwise it logs a warning and starts fresh. stored-state.test.ts writes a V8-serialized v1 object into the DO SQLite under --persist-to, restarts, and checks that the replay is a valid v2 state (no gap/side), PIN 1234 is still enforced (auth error without it), createdAt survives, and the alarm re-arms instead of throwing.
  - Round 2 nit: main-rebase.test.ts drives the real main.ts in jsdom with a fake socket. An offline tap on RIGHT 3 over [2,1,0] and a replay of [0,0,0] sends exactly one `cars` [0,0,3]; the next online tap sends [1,0,3].
  - Round 1 nits: queue all-blank bypass; manifest text; ≈ layout numbers.
verified: `npm run typecheck` exit 0; `npm test` exit 0, 38 files / 266 tests (stored-state and main-rebase
  included); lint and sync:agents:check exit 0 at 3bc4e2e. Earlier passes: check:pins, both builds and `wrangler types --check` all
  exit 0; stale-clear.test.ts 3 runs at 6002–6005 ms; layout 84.95/76.95/70.95 px, no scroll; spotter JS 6.13 KB gz.
range: e544cd2..3bc4e2e
```

Auditor: integrated, 266/266 + every gate exit 0 on master; rendered goldens to `docs/reviews/hud-design-round3.png`; spotter console verified at 390×664 in real Chromium. Open nits → T054 housekeeping: v1 prose in AGENTS.md, constitution §3, hud-e2e-testing skill, role files; `HUD_GAP_FLUSH_MS` rename; drag across segments; PWA icon art.
