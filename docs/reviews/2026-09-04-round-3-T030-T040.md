# Reviews — 2026-09-04 round 3 (T030 glasses app, T040 spotter PWA)

Integrated on master: T030 `a1fea67` + `253a238` + `9fe6c90` (from L017 `66748d2..71403b5`), T040 `9a392a0` + `c000566` (from L018 `f24ecf8..e26eaa5`), plus auditor `90a0e73` (eviction test race). Workers: Opus 5 @ high (consolidated tasks). Reviewer: hud-qa (Opus 5 @ high), fresh context per range. Auditor: Fable 5.1 — sent each worker its review to fix the nits in one follow-up commit, re-ran every gate on the integration branch, rendered the HUD design to PNG and eyeballed it.

## L017 — T030

```
task: T030
reviewer: hud-qa
verdict: approve with nits
findings:
  - [nit] apps/glasses/src/input.ts:133 — `ackedMessageId` latches a msgId forever, but `RoomClient.send` drops an `ack` when the socket is down or not yet replayed (packages/protocol/src/client.ts:257-263) — scenario: spotter sends `m1`; the socket blips (Android suspend); driver taps → the latch is set and the ack frame is discarded; the replayed `state` still has `ackedAt:null`, so `m1` stays on the HUD and every further tap is swallowed for the rest of the session — fix: clear `ackedMessageId` when `onConnection` reports `closed`/`connecting`, or key the guard off the last state-confirmed ack.
  - [nit] apps/glasses/src/driver.ts:89-94 with app.ts:102-108 — on link recovery `watchdog.check()` renders before `applyState`, so an extra HUD job carrying the PRE-frame lane/gap goes out un-dimmed — scenario (measured with a throwaway probe test): the first `state` after connect issues 2 `updateImageRawData` calls (1→3) and a NO-LINK→recovery `state` issues 2 more (4→6); ~370 ms of BLE per event, and for a gap-only recovery the stale gap is shown at full brightness until the debounced second send lands — fix: let `HudApp.setLinkOk` set the flag without rendering when a `state` is about to be applied.
  - [nit] apps/glasses/src/companion.ts:33 and apps/glasses/index.html:88 — `starting…` uses U+2026 outside `glyphs.ts` (030 R6 says every non-ASCII glyph lives there). Phone DOM, never the HUD, so no hardware risk — fix: `glyph('ellipsis')` or plain `...`.
  - [nit] apps/glasses/src/startup-page.ts:41 — `STATUS_ALERT_TEXT_COLOR` is exported and never used; the skill's "status `textColor: 4` when NO LINK" is unimplemented because `textContainerUpgrade` carries no colour and the app deliberately never rebuilds for text — fix: drop the constant or record the deviation in the skill/spec.
verified:
  - Gates in /home/claude/wt/L017, all first-run, no flakes: `npm run typecheck` exit 0; `npm test` exit 0 — 30 files / 171 tests passed (56 s); `npm run lint` exit 0 (0 errors; 2 pre-existing warnings in services/relay/src/worker-configuration.d.ts); `npm run check:pins` exit 0; `npm run build -w apps/glasses` exit 0.
  - Verifiers are real, not placeholders. draw-hud goldens eyeballed: TOP_50 apex-up + 36/72 bar, MID_62 disc + 45/72, BOT_95 apex-down with inverted bar (outline `#`/level 15, fill level 8 asserted numerically), NULL_0 blank symbol with ticks at chars 18/36/54 (x=72/144/216), DIM variant all `+` plus a per-pixel `>> 1` assertion. gray4: 20 736 bytes, stride 144, x=0 → `0xf0` in byte 0 of every row, x=1 → low nibble. render-text: exact AC-1/AC-2 strings incl. `!!` at 90 and blank lane line. queue/queue-image: 10 gap states in 250 ms → 1 send last-wins; 20 in 1 s → ≤5; lane and linkOk bypass; paused bridge proves one call in flight; msg→3, status→4; `{call, ms, result}` asserted. fallback: 3× `sendFailed` → one rebuild, `imageObject` undefined, later HUD updates are `textContainerUpgrade` on container 2; non-consecutive and non-`sendFailed` results correctly do not trip it. link-watchdog and ack drive a real `RoomClient` over a fake socket.
  - Invariants: `app.ts:164-171` returns `''` whenever `ackedAt !== null`; `CLICK_EVENT` only sends `ack` (input.ts:131-138); `lastFrameAt === undefined` → NO LINK (link.ts:38) and a bare `pong` restores `LINK OK · …` (link-watchdog.test.ts:118); close 4401 leaves exactly one socket, no spin; one memoised `createStartUpPageContainer` with no retry; no `localStorage` in the app (bridge KV only); HUD output is ASCII-only, `·`/`…` sit behind `glyphs.ts` with a proven one-call fallback.
  - Protocol fidelity: `HUD_GAP_FLUSH_MS`, `DRIVER_NO_LINK_MS`, close codes, `Lane`/`State`/`ErrorCode` all imported from `@g2-race-spotter/protocol`; `RoomClient` consumed, not re-implemented; `onConnection(state, {code, terminal})` used. Storage keys match the skill exactly.
  - Render mode: URL > KV > `image`; a source-text assertion proves `mode.ts` contains no `import.meta`, and no file under `apps/glasses/src` does; text mode builds slot 2 as text with no image container.
  - SDK (`even-bridge.ts`) checked against `node_modules/@evenrealities/even_hub_sdk/dist/index.d.ts` and the 0.0.12 bundle: `TextContainerProperty.toJson()` and the page `toJson()` spread `this`, so the post-construction `textColor` survives; `ImageRawDataUpdate.toJson()` is `{...this, compressMode: 2, imageData: number[]}`, so `imageWidth`/`imageHeight` pass through and the forced `compressMode` matches `sdk-quirks.ts`; `StartUpPageCreateResult.success === 0` (numeric) matches `START_UP_PAGE_SUCCESS`; `ImageRawDataUpdateResult` string members match `ImageSendResult`; `OsEventTypeList` ordinals 0–8 match `BY_ORDINAL` exactly; container rect 288×144 is inside the SDK's 20–288 / 20–144 range; z-orders 1/3/4/5 are unique and set on every container. Nothing found that would throw or no-op on the real bridge.
  - Scope: `git diff --name-only 260a97e..eab8d75` is entirely under `apps/glasses/**`; nothing at the repo root changed, and the only manifest edit is the `file:` link to `@g2-race-spotter/protocol`.
range: 260a97e..eab8d75
```

## L018 — T040

```
task: T040
reviewer: hud-qa
verdict: approve with nits

findings:
  - [nit] apps/spotter/src/ui/view.ts:236-250 + src/ui/vdom.ts:126-134 — the reconnect banner is a *conditional* child of `.console`, so toggling it shifts every later sibling by one and the positional diff replaces `<main>` wholesale (tag mismatch → `replaceChild`). Chips are safe (verified: adding a chip keeps the message input's node and focus), the banner is not — scenario: spotter is mid-drag on the slider when the first `state` replays and the banner is removed; the `<input type=range>` node is detached under the finger, the drag dies, and the `change` event fires on a detached node so the delegated listener never runs and `gapThrottle.release(final)` is never sent (R3's "once on release"); typing a message when the socket drops loses focus and the mobile keyboard the same way. Measured under jsdom: same range node `false`, same input node `false`, still focused `false` — fix: always render the slot (`reconnectBanner` returns a `hidden` placeholder div instead of `null`) so the child count is constant.
  - [nit] apps/spotter/src/main.ts:282 — the only self-driven re-render is the `PING_INTERVAL_MS` (2 s) tick, but `selectedLane` expires the optimistic highlight at `OPTIMISTIC_LANE_MS` (300 ms). Scenario: socket closed, spotter taps ▲ (intent queued by `RoomClient`); no `state` frame follows, so the button stays lit for up to ~2.3 s instead of reconciling at 300 ms — fix: `window.setTimeout(() => update({}), OPTIMISTIC_LANE_MS)` in `setLane`.
  - [nit] apps/spotter/src/main.ts:90-103 — only `CLOSE_CODE_AUTH` is handled as terminal. A 4400/4426 close leaves `conn:'closed'` with the RECONNECTING banner up forever while `RoomClient` deliberately never reconnects — fix: branch on `detail.terminal` and show a "reload / version mismatch" notice.
  - [nit] apps/spotter/src/net/room-client.ts:104-110 — the tee calls `options.onLatency` *before* forwarding to `RoomClient`; a throw there swallows that frame (pongs only, so `lastFrameAt` misses a liveness refresh). Order, identity guard and malformed frames are all fine (JSON.parse is in a try/catch, the observer is synchronous and never filters) — fix: wrap the `onLatency` call in try/catch.
  - [nit] apps/spotter/test/slider.test.ts — drives the real `createGapThrottle` with a fake clock and counts real `send` calls (t=0/100/200 + release = 4, last = 30 ✓), but the input→`input()` / change→`release()` wiring in main.ts:241-261 is untested because main.ts is import-unsafe (top-level `getElementById`). A regression there would pass AC-2 — fix (later task): move the bootstrap into an exported `start(root)`.
  - [nit] apps/spotter/src/sw.ts:16-21 — PRECACHE lists only `/`, the manifest and the icons; the hashed JS/CSS are cache-first on demand, so the first offline launch after install has a shell and no app. The `spotter-ui` skill asks for the built shell incl. JS/CSS to be precached.

verified:
  - Gates in the worktree, all exit 0: `npm run typecheck`; `npm test` → 22 files / 136 passed (no eviction flake this run, first attempt, so no re-run needed); `npm run lint` (2 pre-existing warnings in services/relay/src/worker-configuration.d.ts, untouched by this diff); `npm run check:pins` ("Dependency pins are exact"); `npm run build -w apps/spotter` → main-*.js 17.08 kB / **5.97 kB gzipped** vs the 40 KB budget, plus sw.js 1.12 kB, at `apps/spotter/dist` where `services/relay/wrangler.jsonc` points.
  - AC-1/3/4 `view.test.ts` (jsdom, real `createRenderer` patch path across frames), AC-2 `slider.test.ts` (fake clock, real throttle), AC-5 `bundle-size.test.ts` (spawns the real `vite build` via `execFileSync` and gzips every JS chunk in dist/assets), `recent-messages.test.ts`, `room-client.test.ts` — all present and running under root `npm test`.
  - Invariants: no re-send of a delivered `msg` anywhere in the app; lane/gap taps while closed go to `RoomClient.send` and are queued there (client.ts:queue); `onState` takes the replayed server state as truth (main.ts:83-88, gap held only while `draggingGap`); banner + enabled controls on `closed`; terminal 4401 → `disconnect()`, "wrong PIN", back to Join, no reconnect spin.
  - Protocol fidelity: `GAP_SEND_MIN_MS`, `MSG_MAX_CHARS`, `PING_INTERVAL_MS`, `CLOSE_CODE_AUTH` all imported from `@g2-race-spotter/protocol`; nothing re-typed; `packages/protocol` untouched. The injected WebSocket ctor only tees `message` events synchronously and forwards `send`/`close`/`addEventListener`, so `RoomClient`'s `this.socket !== socket` guard still compares the instance it constructed.
  - Skill compliance: lane ≥ 88 px (64 px landscape, styles.css:195/377), range height 44 px, chips 0·25·50·75·100, `maxlength = MSG_MAX_CHARS`, 5 deduped chips in `g2rs:v1:recentMsgs`, header amber offline / red down, EWMA α=2/6 over 5 samples greyed past 10 s, `env(safe-area-inset-*)`, `navigator.vibrate?.(10)` on lane tap and send.
  - PWA: manifest has name/short_name/start_url `/`/display standalone/192+512 incl. maskable; `shouldHandle` excludes `/room/*` and `/health` for any method/origin and is unit-tested; built `dist/sw.js` contains no `import`/`export`, so `register('/sw.js')` as a classic worker is valid.
  - Scope: one commit; only `apps/spotter/**` plus root `package.json` (`jsdom` 30.0.1 exact, authorised by the task line) and the lockfile. No `any`, no re-typed constants, no secrets.

range: 260a97e..f24ecf8
```

## Auditor disposition

- T030 nits 1–4 fixed in `9fe6c90` (ack guard released on state/connection change with a blip-then-tap test; one HUD send per recovery frame with a test; ellipsis behind `glyphs.ts`; unused status colour constant removed and the deviation recorded in the `g2-hud-display` skill).
- T040 nits 1–4 and 6 fixed in `c000566` (constant child slots so a drag/keyboard survives banner toggles, with a node-identity test; optimistic lane expiry timer; all terminal closes handled; try/catch in the pong tee; service worker precaches the built shell). Nit 5 (import-safe bootstrap for `main.ts`) deferred.
- Pre-existing flake in `services/relay/test/eviction.test.ts` (a `peer` broadcast racing the lane frame, ~1 in 5 runs) fixed by the auditor with `nextMessageMatching`.
- Gates on master: typecheck; **205/205** (33 files, live wrangler dev); lint 0 errors / 2 generated-d.ts warnings; prettier clean; pins exact; sync up to date; `build -w apps/glasses` ok (35.6 KB gz); `build -w apps/spotter` ok (6.0 KB gz JS of the 40 KB budget); `wrangler deploy --dry-run` with the real spotter assets ok.
- Post-integration on Maxx's machine: `npm ci` installed only 5 packages because `NODE_ENV=production` is set system-wide there — documented in HANDOFF (`npm ci --include=dev`). With dev deps present, one cross-test race surfaced: the spotter bundle-size test built into `apps/spotter/dist` while the relay roundtrip test asserted those assets were untouched. Fixed by building into a temp `--outDir` (`apps/spotter/test/bundle-size.test.ts`).
