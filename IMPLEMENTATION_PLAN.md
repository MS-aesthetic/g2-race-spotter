# Implementation plan — 2026-09-04T12:30:00-04:00

Status: BUILDING
Current spec focus: specs/020-protocol-and-relay.md (relay stream) and specs/030-glasses-app-text-mode.md (glasses stream)

## Model routing (either vendor per tier — pick per run, mixing is fine)

| Tier | Claude | OpenAI |
|---|---|---|
| worker (every `owner:` task below) | Sonnet 5 @ high | GPT 5.6 Terra @ high |
| reviewer (`protocol-keeper` / `hud-qa`) | Opus 5 @ high | GPT 5.6 Sol @ high |
| planner (`plan-updater`) | Opus 5 @ xhigh | GPT 5.6 Sol @ xhigh |
| auditor (human-invoked final QA) | Fable 5.1 / Opus 5 @ xhigh | GPT 5.6 Sol @ xhigh |

## Active stream leases (interactive coordinator only)

- No active leases. L015 (T009) and L016 (T017) are integrated and closed. Quarantined worktrees L009/L014 are superseded; keep for traceability, never lease from them.
- Two independent streams are open: `relay-room` (T012 → T013 → T014) and `glasses-app` (T020 → T021/T022 → T023 → T024 → T025). A coordinator may run one worker per stream concurrently; the serial runner takes them in the order below.

## Next (ordered; the serial runner takes the first unchecked task)

- [ ] T012 (owner: relay-backend-dev) (spec: 020 AC-7) [stream:relay-room; lock:relay-room; depends:T009 integrated (`b28cc2c`)] Implement silent-peer handling in `alarm()`; verify `services/relay/test/heartbeat.test.ts` (spotter stops pinging → driver sees `spotterOnline === false` and the spotter socket closes 4408 within `PEER_OFFLINE_MS + ALARM_TICK_MS`). Traps: reduce `{t:'peer', online:false}` inline and broadcast *before* `close()` (server close does not fire `webSocketClose`); **add `attachment.rejected !== true` to the `persistAndBroadcast` and `hasReadyPeer` predicates** (T009 reviewer nit — an evicted socket that is still in `getWebSockets()` must never be counted or sent state); exclude sockets already closed; **reap unready or roleless sockets that never sent `hello`** — `fetch()` arms no alarm at accept time (L014 choice, kept by T011d), so either arm on accept or reap on the next tick and update the `alarm: null` assertions in `services/relay/test/auth.test.ts` *deliberately* (they encode a temporary choice, not a spec rule — say so in the hand-off); self-arm at the end of `alarm()` while sockets remain; timings imported from `packages/protocol`, never re-typed. Extra gates: pinned live Wrangler test, every root gate, `npx wrangler types src/worker-configuration.d.ts --check`, isolated-assets deploy dry-run.
- [ ] T020 (owner: g2-glasses-dev) (spec: 030 AC-1, AC-2; R6) [stream:glasses-app; lock:glasses-render] Create `apps/glasses/src/render/glyphs.ts` (every non-ASCII glyph `▲ ● ▼ █ ░ · …` behind one module with a per-glyph ASCII fallback switch: `█`→`#`, `░`→`-`, `·`→`|`, `…`→`...`) and `apps/glasses/src/render/text.ts` exporting pure `renderText(state: State): string` per the `g2-hud-display` skill: line 1 = lane glyph or blank, line 2 = 20-cell bar (`filled = Math.round(gap / 5)`) + two spaces + integer gap, `!!` prefix at `gap ≥ 90`; verify `apps/glasses/test/render-text.test.ts` (AC-1 exact string for `lane:"top", gap:62`; AC-2 `!!` at 90/100 and blank first line for `lane:null`; the ASCII-fallback variant of the same states). `State` type imported from `@g2-race-spotter/protocol`, never re-typed. Also add `apps/glasses/test` to the root test discovery if `startup-page.test.ts` is not already picked up by `npm test` (check `vitest.config.ts` first). Extra gates: every root gate.
- [ ] T021 (owner: g2-glasses-dev) (spec: 030 AC-6; R2) [stream:glasses-app; lock:glasses-startup; depends:T020 integrated] Add `resolveRenderMode({ search, kv })` (`?render=` → KV `g2rs:v1:render` → `'image'`; never inspects `import.meta.env.MODE`) in `apps/glasses/src/render/mode.ts` and make `startup-page.ts` build slot 2 as a text container (`textColor: 4`, same rect) in text mode and as the (still blank) image container otherwise — one `createStartUpPageContainer` call either way; verify `apps/glasses/test/render-mode.test.ts` (URL wins over KV; KV `text` alone; neither → image, including under `--mode simulator`; exactly one startup call; text mode never creates an image container). Do not touch `render/queue.ts`. Extra gates: every root gate, `npm run build -w apps/glasses`.
- [ ] T022 (owner: g2-glasses-dev) (spec: 030 AC-3; R7) [stream:glasses-app; lock:glasses-queue; depends:T020 integrated] Create `apps/glasses/src/render/queue.ts`: single async worker, at most one bridge call in flight, a new `hud` job replaces a pending `hud` job (latest wins), gap-only changes debounced to a `HUD_GAP_FLUSH_MS` floor while lane changes bypass the debounce, `msg` → `textContainerUpgrade` on container 3, `status` → container 4, every bridge call timed with the injected clock and logged as `{call, ms, result}`, consecutive `sendFailed` counted (exposed, not acted on — the image fallback is 050); verify `apps/glasses/test/queue.test.ts` with a mocked bridge and fake timers (AC-3: 10 gap states in 250 ms → ≤ 1 HUD upgrade per window, last value wins; lane change flushes immediately; in-flight call serialises the next). Constants from `@g2-race-spotter/protocol`. Extra gates: every root gate.
- [ ] T023 (owner: g2-glasses-dev) (spec: 030 AC-4; R3) [stream:glasses-app; lock:glasses-status; depends:T022 integrated; depends:T017 integrated (`c9fb8b6`)] Link watchdog + status strip in `apps/glasses/src/link.ts` (or `status.ts`): reads `RoomClient.lastFrameAt` on an injected timer; `NO LINK` + dimmed HUD when `lastFrameAt === undefined` or `now - lastFrameAt > DRIVER_NO_LINK_MS` (the 2026-09-04 spec 030 decision: per-session, dims during a reconnect blip — do not cache the previous session's timestamp); recovers on the next frame of any kind; status strings exactly as the skill lists (`LINK OK · SPOTTER ON/OFF`, `NO LINK`, `CONNECTING…`, `ROOM ?`); `closed` with `terminal: true` shows a terminal status (no auto-reconnect); verify `apps/glasses/test/link-watchdog.test.ts` (AC-4 5 s silence → `NO LINK` and the next HUD job is the dimmed variant; a `pong`-driven `lastFrameAt` advance restores `LINK OK · …`). Extra gates: every root gate.
- [ ] T024 (owner: g2-glasses-dev) (spec: 030 AC-5; R4, R8) [stream:glasses-app; lock:glasses-input; depends:T023 integrated] Input mapping in `apps/glasses/src/input.ts` with a `toOsEvent(event)` normaliser: `CLICK_EVENT` → `client.send({t:'ack', msgId})` for the current unacked message only, never clears locally; `DOUBLE_CLICK_EVENT` → `shutDownPageContainer(1)`; `FOREGROUND_ENTER_EVENT` → `client.connect(url)` (debounced ≥ 1 s — a foreground burst must not tear down a handshake that is awaiting replay; T017 reviewer nit) + re-render last state; exit events close the socket; verify `apps/glasses/test/ack.test.ts` (AC-5: ack sent, text stays until a `state` with `ackedAt` arrives, then hides; second click with no unacked message sends nothing; double click calls `shutDownPageContainer(1)` once). Extra gates: every root gate.
- [ ] T025 (owner: g2-glasses-dev) (spec: 030 R5, R7; wires AC-1–AC-6 together) [stream:glasses-app; lock:glasses-main; depends:T024 integrated] Companion page + `main.ts` wiring: join form (room / PIN / name) persisted to bridge KV keys `g2rs:v1:room|pin|name|render` from the protocol skill, log panel, `RoomClient` constructed with the browser `WebSocket`, store → queue → renderer; verify `apps/glasses/test/settings.test.ts` (KV round-trip, `ROOM ?` when no room, URL built as `wss://<host>/room/<ROOM>?role=driver&token=&name=`) and `npm run build -w apps/glasses`. Extra gates: every root gate.
- [ ] T013 (owner: relay-backend-dev) (spec: 020 AC-9; requirement R4) [stream:relay-room; lock:relay-room; depends:T012 integrated] Implement UTF-8 byte cap (`FRAME_MAX_BYTES` measured before `JSON.parse`), malformed/unknown-`t`/wrong-role handling, and fixed-window rate limiting; verify `services/relay/test/validation.test.ts` and `services/relay/test/ratelimit.test.ts`. Preserve version precedence (`7bab61a`), keep non-hello malformed sockets open with `error{bad_frame}`, cap applied frames at `RATE_BURST` per 1 s window with at most one `error{rate}` per window, normalize `name=''` to absent. Extra gates: pinned live Wrangler tests, every root gate, Wrangler types check, isolated-assets deploy dry-run.
- [ ] T014 (owner: relay-backend-dev) (spec: 020 AC-10; requirement R6) [stream:tooling; lock:fake-spotter; depends:T009 integrated] Implement all fake-spotter scenarios (`lanes`, `gap-sweep`, `message-ack`, `link-loss`, `reconnect-replay`, `driver-evict`, `soak`) and `--role driver` in `scripts/fake-spotter.ts`, reusing protocol fixtures and `RoomClient` (`onError` + `{code, terminal}` from T017 for the exit code); verify `scripts/test/fake-spotter.test.ts` against local Wrangler (arm the evicted driver's no-message watch *before* the second driver joins — T009 reviewer nit); the deployed proof remains T102 `[HW]`.
- [ ] T018 (owner: relay-backend-dev) (spec: 060 R3 verifier hardening) [stream:protocol-client; lock:room-client; depends:T017 integrated] Extract `packages/protocol/test/support/fakes.ts` (FakeSocket/FakeWebSocket/FakeTimers) and use it from both client test files; add to `client-errors.test.ts`: numeric-close path (`on('close', 4409)`) carries `{code: 4409, terminal: true}`; `onError` fires before the `closed` callback for the relay's error-then-close sequence; a throwing `onError`/`onState` listener does not stop the other listeners (wrap listener dispatch in try/catch in `client.ts`, report via `console.error` — no rethrow). No behaviour change beyond the try/catch. Extra gates: every root gate.

## Needs simulator [SIM]

- [ ] T002c (owner: hud-qa) (spec: 010 AC-3) [SIM] Run `npm run sim:scenarios -- --smoke` on an interactive machine where simulator 0.9.5 creates its main window; commit `qa/<date>/sim/report.json` and `qa/<date>/sim/image/smoke-01.png`, including the actual simulator `getDeviceInfo()` value.
- [ ] T003c (owner: relay-backend-dev) (spec: 010 AC-5) After T002c records the exact simulator `bridge.getDeviceInfo()` value, remove every non-hardware `TBD` allowance, make the valid fixture/test reject non-hardware `TBD`, add the root environment check to CI, and validate `engines.node` permits only major 22; verify `scripts/test/check-environment.test.ts`, the repository check, and full AC-1 gates.
- [ ] T026 (owner: hud-qa) (spec: 030 AC-7) [SIM; depends:T025 integrated] Run `npm run sim:scenarios` for `lanes`, `gap-sweep`, `message-ack`, `link-loss`, `reconnect-replay` in text mode (`?render=text`) against a local Wrangler relay driven by `fake-spotter`; commit `qa/<date>/sim/text/*.png` and `qa/<date>/sim/report.json`. Parks here until an interactive simulator window exists (same blocker as T002c).

## Needs human [HW]

- [ ] T101b (owner: maxx) (spec: 010 AC-4) Enable Developer Mode, QR-sideload the scaffold, and save the required photo/log under `qa/<date>/`.
- [ ] T102 (owner: maxx) (spec: 020 AC-10) Deploy the Worker, set `DEBUG_KEY`, run `lanes` with spotter and driver clients, and save `qa/<date>/020-deploy.log`. (Needs T014 first.)
- [ ] T104 (owner: maxx) (spec: 030 AC-8) Run real-glasses `lanes`, `gap-sweep`, `message-ack`, and `link-loss`; record PASS by AC in `qa/<date>/REPORT.md`.
- [ ] T105 (owner: maxx) (spec: 030 AC-9) Render the glyph sheet on real glasses and record visibility/fallback choices in `docs/ENVIRONMENT.md`.
- [ ] T106 (owner: maxx) (spec: 030 AC-10) Verify the hardware `wss://` upgrade against the whitelisted origin and record the result in `docs/ENVIRONMENT.md`.
- [ ] T107 (owner: maxx) (spec: 040 AC-6) Verify install, join, console, and landscape targets on iOS Safari and Android Chrome; save `qa/<date>/040-*.png`.
- [ ] T108 (owner: maxx) (spec: 040 AC-7) Run two phones plus real glasses over LTE and record the lane-call latency in `qa/<date>/REPORT.md`.
- [ ] T109 (owner: maxx) (spec: 050 AC-6) Send the gray4 bright-column test pattern and record nibble/stride confirmation in `docs/ENVIRONMENT.md`.
- [ ] T110 (owner: maxx) (spec: 050 AC-7) Run `gap-sweep` plus a 10-minute soak on real glasses; save `latency.csv` and the AC result in `qa/<date>/REPORT.md`.
- [ ] T111 (owner: maxx) (spec: 050 AC-8) Open and cancel the exit dialogue and verify text fallback within three failed image sends in `qa/<date>/REPORT.md`.
- [ ] T112 (owner: maxx) (spec: 060 AC-5) Perform the five-minute iOS lock/unlock recovery check and record it in `qa/<date>/REPORT.md`.
- [ ] T113 (owner: maxx) (spec: 060 AC-6) Perform the five-minute Android lock/foreground recovery check and record it in `qa/<date>/REPORT.md`.
- [ ] T114 (owner: maxx) (spec: 060 AC-7) Redeploy the relay mid-session and record automatic recovery in `qa/<date>/REPORT.md`.
- [ ] T115 (owner: maxx) (spec: 060 AC-8) Save a session `wrangler tail` showing parseable structured lines and no message bodies to `qa/<date>/tail.log`.
- [ ] T116 (owner: maxx) (spec: 070 AC-4) Follow `docs/RACE_DAY.md` before grid and record real `lanes` plus `message-ack` PASS in `qa/<date>/REPORT.md`.
- [ ] T117 (owner: maxx) (spec: 070 AC-5) Archive full-session logs, latency table, defects, and open-question disposition under `qa/<date>/` with human sign-off.

## Done this cycle

- [x] T009 (owner: relay-backend-dev) (spec: 020 AC-5) L015 `697ad4c` → `b28cc2c`. Eviction in the `!ready` hello branch after the PIN compare, before ready; every ready non-rejected driver except the joiner gets `error{role_taken}` + 4409 via `rejectSocket`; wrong PIN / bad version never evict. Review: approve with nits (`docs/reviews/2026-09-04-round-2-T009-T017.md`). `services/relay/test/eviction.test.ts` live.
- [x] T017 (owner: relay-backend-dev) (spec: 060 R3; 030 R3) L016 `46afe01` → `c9fb8b6` + auditor `8bf5531`. `onError(cb)`; `onConnection(state, {code, terminal})`; terminal close clears queued intents, `lastFrameAt`, schedules no reconnect; `connect()` same-URL no-op only after replay. Review: approve with nits. `packages/protocol/test/client-errors.test.ts` (6 tests).
- [x] docs `d633a5d` — Claude and OpenAI models listed per tier (constitution, plan, AGENTS.md, README, models.env); auditor tier added. `9fd612b` prettier fix in `auth.test.ts`. `d447981` stale `_to_delete/` removed.
- [x] Earlier this cycle: T011 chain → `7bab61a` + `4181e34` (020 AC-6); T006 chain → `68d9776` + `fa63af2` + `c1218e7` (020 AC-8); `a88832c` sync-agents orphan rule; T016 `0909521`; T008/T008a, T007a/T007c, T007/T007b, T004–T004c, T005/T005a — see `ralph/PROGRESS.md`.

## Notes / why

- 2026-09-04 round 2 (Fable 5.1 coordinating; Sonnet 5 @ high workers in parallel container worktrees; Opus 5 @ high fresh-context reviewers on exact ranges; serial integration with every root gate after each). Gates at `d447981`: typecheck; 92/92 (18 files, live wrangler dev); lint 0 errors / 2 generated-d.ts warnings; prettier clean; pins; sync; `wrangler types --check`; isolated-assets dry-run.
- Two streams are now open because the client is integrated: the relay stream continues T012 → T013 → T014; the glasses stream starts at T020. T020 is listed second so the serial runner alternates streams and a coordinator can run both at once. Glasses tasks are ordered by data flow (pure render → mode/startup → queue → watchdog → input → wiring) so each test only needs the modules before it.
- T012 absorbs the T009 reviewer nit (`rejected` filter in `persistAndBroadcast`/`hasReadyPeer`) rather than reopening T009 — same file, same verifier area, and T012's silent-peer reaper is where a stale-but-present socket first matters.
- T018 collects the T017 verifier nits (numeric-close detail, error-before-closed ordering, throwing listeners, shared fakes) in one write scope; none affects a spec criterion, so it sits after T014.
- `connect()` now retires an open-but-unreplayed socket on purpose (reviewer nit 5): correct for a zombie, wasteful on a slow link. Consumers debounce foreground `connect()` — written into T024 and to be repeated in the 040/060 slices.
- Process lesson: never grep a gate's output for "error"; read the exit code. The Windows lint run hid a prettier failure for two iterations.
- `ralph/loop.sh` still honours only `owner:` and `[HW]`; `depends:`/`lock:` are honoured by agents reading the plan — keep dependent tasks ordered (T021/T022 after T020, T023 after T022, T024 after T023, T025 after T024, T013 after T012).
- The three untracked `.claude/agents/cavecrew-*.md` files are Maxx's; never delete, commit, or regenerate over them.
- AC audit 010: AC-1/AC-2/AC-6/AC-7 met; AC-3 and AC-5 need simulator; AC-4 needs hardware.
- AC audit 020: AC-1–AC-6, AC-8 met; AC-7 (T012), AC-9 (T013) open; AC-10 `[HW]` after T014.
- AC audit 030: AC-1–AC-6 sliced as T020–T025; AC-7 `[SIM]` T026; AC-8–AC-10 `[HW]`. 040–070: future work.
- No criterion is `DISPUTED`; no simulator or hardware evidence is claimed; no deployment or secrets.
