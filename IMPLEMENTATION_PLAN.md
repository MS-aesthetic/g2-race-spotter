# Implementation plan — 2026-09-04T17:30:00-04:00 (consolidated)

Status: BUILDING
Current spec focus: hardware/simulator evidence (Maxx) + specs/020 AC-7/AC-9 (T050)

## Model routing (either vendor per tier — pick per run, mixing is fine)

| Tier | Claude | OpenAI |
|---|---|---|
| worker — small task | Sonnet 5 @ high | GPT 5.6 Terra @ high |
| worker — consolidated task (T030/T040/T050 below) | Opus 5 @ high | GPT 5.6 Sol @ high |
| reviewer (`protocol-keeper` / `hud-qa`) | Opus 5 @ high | GPT 5.6 Sol @ high |
| planner (`plan-updater`) | Opus 5 @ xhigh | GPT 5.6 Sol @ xhigh |
| auditor (human-invoked final QA) | Fable 5.1 / Opus 5 @ xhigh | GPT 5.6 Sol @ xhigh |

## Consolidation (Maxx, 2026-09-04)

The one-criterion-per-iteration slicing cost ~3 iterations per task and the app is small. From here: **one task per app or subsystem**, a strong worker, one exact-range review per task, the auditor over the batch. Image HUD first (no text-first phase); text is an ASCII fallback. Rate limiting dropped; fake-spotter scenarios, client verifier polish (old T018), 060 hardening and 070 packaging deferred until after the first hardware session. Simulator runs are Maxx's (`npm run sim:scenarios` on his machine) once T030 lands.

## Active stream leases (interactive coordinator only)

- L019 — T052 — integrated as `56d979b` + `510bd90` + `b07aad5` (review: approve with nits, `docs/reviews/2026-09-04-round-4-T052.md`). Lease closed.
- L020 — T053 — base `b07aad5` — write scope: `apps/spotter/**`, `apps/glasses/src/{app,driver,input}.ts` + tests, `.agents/skills/{spotter-ui,g2-hud-display}/SKILL.md`, specs 030/040 Decisions, root `package.json`/lock only for a pinned `playwright-core` devDependency — lock: app-design — worker: spotter-pwa-dev (Opus 5 @ high) — reviewer: hud-qa (Opus 5 @ high) — auditor: Fable 5.1. — write scope: `packages/protocol/**`, `services/relay/src/race-room.ts` + relay tests only if needed, `apps/glasses/**`, `apps/spotter/**`, `.agents/skills/{race-relay-protocol,g2-hud-display,spotter-ui}/SKILL.md` (doc updates to match), specs Decisions only — lock: everything app-side (no parallel lease) — worker: g2-glasses-dev (Opus 5 @ high) — reviewer: hud-qa + protocol-keeper (Opus 5 @ high) — auditor: Fable 5.1.

## Next (ordered; the serial runner takes the first unchecked task)

- [ ] T055 (owner: g2-glasses-dev) (Maxx design round 3, 2026-09-25) [stream:app-design; lock:app-design; depends:T053 integrated] Maxx verbatim: "Right now icons are too large on the glasses. make them 1/2 current size. replace middle circle with dash/rectangle. move icons to top of screen for top mid bottom. on bottom of screen have 3 hollow bars. left, middle, right these represent cars behind and closeness. make UI have 3 sliders with 3 segments each. Clear glasses screen after 6 seconds without data / update from app." Design decisions: (a) **Protocol v2**: `gap` and `side` are REMOVED; new `State.cars: [left, mid, right]` each an integer 0–3 (0 = no car, 3 = on the bumper), initial `[0,0,0]`; spotter-only `{t:'cars', cars:[l,m,r]}` (full triple, server clamps+rounds, no-op does not bump `seq`); `RoomClient` queues the latest `cars` while offline like `lane`; new reducer event `{t:'stale'}` → `lane:null, cars:[0,0,0], msg:null` (online flags kept), bumps `seq`; exported `HUD_STALE_CLEAR_MS = 6000`; `PROTOCOL_VERSION = 2` (fixtures, hello, tests updated; a v1 client gets `error{version}` + 4426 and the spotter page already shows the reload notice). (b) **Relay auto-clear**: `scheduleAlarm` also considers `updatedAt + HUD_STALE_CLEAR_MS` while the state is non-empty (lane, any car, or msg) and takes the EARLIER of that and the existing tick/TTL target — an alarm may be moved earlier by a state change, never later (the T016 rule stands: pings never re-point it); `alarm()` reduces `{t:'stale'}` + persists + broadcasts when `now - updatedAt >= HUD_STALE_CLEAR_MS` and the state is non-empty. Verify in `services/relay/test/stale-clear.test.ts` (live: spotter calls lane top + cars, goes quiet → driver receives the cleared `state` between 6 and 7 s; a fresh call within 6 s restarts the window; an empty room never loops). Do NOT touch the silent-peer/rate work (T050). (c) **Glasses page**: HUD image container at the TOP (y 8), `msg` text container below it (y ~160, h ~90), `status` bottom-right unchanged. (d) **HUD image (288×144)**: lane icons across the TOP row at HALF the current size (▼ left, ▬ dash/rectangle middle ~40×10, ▲ right; triangles ~30 px tall), active one dithered-filled, inactive as thin outlines, `lane:null` → all outlines; the BOTTOM of the image has three hollow bars side by side (left / middle / right, ~88×28 each, 2 px outline, level 6) each divided into 3 segments; `cars[i]` segments are dithered-filled bottom-up/left-to-right (say which and keep it in `hud-design.ts`); level 3 gets the bright outline (alert). NO-LINK `>> 1` unchanged. Re-record goldens: lanes ×4, cars `[0,0,0]`, `[1,2,3]`, `[3,0,0]`, dim. Text fallback: line 1 `v - ^` markers, line 2 three bracketed 3-cell bars `[## ] [#  ] [###]`. (e) **Spotter**: replace the gap buttons AND the inside/outside buttons with three rows LEFT / MIDDLE / RIGHT, each a 3-segment tappable bar (segments 1·2·3, tap segment n → level n, tap the lit top segment → 0; drag across segments optional); one `cars` send per change with the full triple; selected state from server; still no scrolling at 360×640 (layout test updated; lane buttons may shrink to 64 px). (f) Docs: update `race-relay-protocol` (messages, State, reducer, timings incl. `HUD_STALE_CLEAR_MS`, versioning note "v2 2026-09-25"), `g2-hud-display` (layout table, bitmap), `spotter-ui` (rows); Decisions "2026-09-25 (Maxx) design round 3" in 020/030/040/050; rewrite 040 AC-2 and 050 AC-1/AC-2/AC-4 to the new elements (human-authorized). Verifiers: protocol guards/reduce/client tests, `stale-clear.test.ts`, `draw-hud.test.ts`, `render-text.test.ts`, `view.test.ts`, `intents.test.ts`, `layout.test.ts`. Extra gates: every root gate, both builds, `wrangler types --check`, isolated-assets dry-run. Note for the worker: only ONE image container (≤ 288×144) exists, so "top of screen" = top of the image at y 8 and "bottom" = bottom of the image (screen y ~150); the message text sits below it. Record that constraint in the 050 Decision so Maxx can choose otherwise.


- [ ] T052 (owner: g2-glasses-dev) (Maxx design round 1, 2026-09-04 — human-requested changes to 030/050/040; record as Decisions) [stream:app-design; lock:app-design; depends:T030+T040 integrated] **HUD redesign + "car inside/outside" signal.** (a) Protocol: additive `State.side: 'inside' | 'outside' | null` and spotter-only message `{t:'side', side}`; guards, fixtures (`side.valid/invalid.json`), reducer (no-op does not bump `seq`), `RoomClient` queues the latest `side` while offline like `lane`; `PROTOCOL_VERSION` stays 1 (additive). Relay: accept via `isClientMessage` (spotter role only). (b) Glasses HUD image (288×144, still one image container): three lane positions across the top — ▼ left, ● middle, ▲ right — active one filled with a checkerboard/Bayer dither (easier on the eyes), inactive ones as faint outlines; bar below; under the bar a ◀ at the left edge when `side === 'inside'` and a ▶ at the right edge when `side === 'outside'`; all fills dithered, outlines solid; ≥ 90 alert and NO-LINK dimming kept. Page layout: message text container at the TOP of the canvas, image below it, status at the BOTTOM RIGHT. (c) Status strip becomes two letters: `L` solid when the link is OK, blinking (~700 ms via `status` jobs, never image sends) when NO LINK; `S` present only while `spotterOnline`; `ROOM ?` unchanged. (d) Spotter PWA: two toggle buttons **CAR INSIDE** / **CAR OUTSIDE** under the slider (tap again to clear; reflect server state); header unchanged. (e) Update the three skills and re-record goldens; add Decisions to specs 020/030/050/040. Verifiers: existing suites updated + `packages/protocol/test/reduce.test.ts` side cases, `apps/glasses/test/draw-hud.test.ts` new goldens (each lane, each side, dither present, dim), `apps/glasses/test/link-watchdog.test.ts` L/S strings + blink, `apps/spotter/test/view.test.ts` side buttons. Extra gates: every root gate, both app builds.

- [ ] T050 (owner: relay-backend-dev, small enough for a Sonnet-tier worker) (spec: 020 AC-7, AC-9; R4 minus rate limiting) [stream:relay-room; lock:relay-room; depends:T009 integrated (`b28cc2c`)] **Relay heartbeat + validation in one task.** `alarm()`: reduce `{t:'peer', online:false}` inline and broadcast *before* `close(4408)` (server close does not fire `webSocketClose`); add `attachment.rejected !== true` to `persistAndBroadcast`/`hasReadyPeer`; reap unready/roleless sockets that never sent `hello` (arm on accept or reap next tick — update the `alarm: null` assertions in `auth.test.ts` deliberately and say so); self-arm while sockets remain. Validation: `FRAME_MAX_BYTES` on UTF-8 byte length before `JSON.parse`; unknown `t` ignored and counted; malformed non-hello → `error{bad_frame}`, socket stays open; wrong-role frames ignored; `name=''` → absent; version precedence (`7bab61a`) preserved. **Verifiers:** `services/relay/test/heartbeat.test.ts` (spotter stops pinging → driver sees `spotterOnline === false` and spotter closes 4408 within `PEER_OFFLINE_MS + ALARM_TICK_MS`; an unready socket is reaped), `validation.test.ts` (AC-9). Extra gates: pinned live Wrangler tests, every root gate, `wrangler types --check`, isolated-assets dry-run.
- [ ] T051 (owner: g2-glasses-dev) (spec: 050 image polish) [depends:T030 integrated; T104/T109 evidence] Whatever the first hardware session shows: nibble order / stride flip, pacing, `sendFailed` behaviour, design tweaks in `hud-design.ts`. Scoped by Maxx after the session; do not start before.

## Needs simulator [SIM] — Maxx runs these on his machine

- [ ] T002c (owner: maxx) (spec: 010 AC-3) `npm run sim:scenarios -- --smoke` with simulator 0.9.5 open; commit `qa/<date>/sim/report.json` + `smoke-01.png` (records the real `getDeviceInfo()` for T003c).
- [ ] T026 (owner: maxx) (spec: 050 AC-5b, 030 AC-7) [depends:T030 integrated; needs a local relay: `npm run dev -w services/relay` + the spotter PWA or a hand-sent frame] `npm run sim:scenarios` in image mode for `lanes`, `gap-sweep`, `message-ack`, `link-loss`, `reconnect-replay`; commit `qa/<date>/sim/image/*.png` + `report.json`. This is the first look at the actual HUD design — expect to iterate `hud-design.ts` afterwards.

## Deferred (not before the first hardware session)

- T003c environment TBD removal (after T002c); T014 fake-spotter scenarios (real phones are the scenario runner); T018 client verifier polish (shared fakes, numeric-close detail, error-before-closed ordering, try/catch around listeners); spec 060 hardening (Android/iOS lifecycle, redeploy recovery, structured tail); spec 070 race-day packaging.

## Needs human [HW]

- [ ] T101b (owner: maxx) (spec: 010 AC-4) Enable Developer Mode, QR-sideload the glasses app, save the photo/log under `qa/<date>/`. **Do this right after T030 — it is the first real information the project gets.**
- [x] T102 (owner: maxx) (spec: 020 AC-10) Deployed 2026-09-04 to `https://g2-race-relay.maxx-384.workers.dev` (account `maxx-384`); `DEBUG_KEY` not yet set; `qa/<date>/020-deploy.log` not yet saved — Maxx to paste the deploy output when convenient.
- [ ] T104 (owner: maxx) (spec: 030 AC-8) Real-glasses `lanes`, `gap-sweep`, `message-ack`, `link-loss` from the spotter PWA; `qa/<date>/REPORT.md`.
- [ ] T106 (owner: maxx) (spec: 030 AC-10) `wss://` upgrade on hardware against the whitelisted origin; note in `docs/ENVIRONMENT.md`.
- [ ] T107 (owner: maxx) (spec: 040 AC-6) Install + join on iOS Safari and Android Chrome; landscape check; `qa/<date>/040-*.png`.
- [ ] T108 (owner: maxx) (spec: 040 AC-7) Two phones + glasses over LTE; lane-call latency in `qa/<date>/REPORT.md`.
- [ ] T109 (owner: maxx) (spec: 050 AC-6) Bright-column test pattern → nibble/stride confirmation in `docs/ENVIRONMENT.md`.
- [ ] T110 (owner: maxx) (spec: 050 AC-7) `gap-sweep` + 10-minute soak; `latency.csv`.
- [ ] T111 (owner: maxx) (spec: 050 AC-8) Exit dialogue → text fallback within 3 sends.
- Deferred with 060/070: T112–T117.

## Done this cycle

- [x] T052 design round 1 (`56d979b`, `510bd90`, `b07aad5`); T053 design round 2 (`c0e5d18`, `b6d94cc`, `9276828`, `50678f2`); T102 first deploy done by Maxx (relay live at `https://g2-race-relay.maxx-384.workers.dev`, redeployed after each round). See `docs/reviews/2026-09-04-round-4-5-T052-T053.md`.
- [x] T030 (g2-glasses-dev) (030 AC-1–AC-6, 050 AC-1–AC-5) `a1fea67` + `253a238` + `9fe6c90` — image-first glasses app: `render/primitives.ts`, `render/hud-design.ts` (the one file to edit for a new look), `draw-hud.ts`, `gray4.ts`, `sdk-quirks.ts`, ASCII `text.ts`, `mode.ts`, `queue.ts`, `link.ts`, `input.ts`, `settings.ts`, companion page, `even-bridge.ts` (only SDK importer). 13 test files. Review approve with nits, nits fixed. Preview: `docs/reviews/hud-design-preview.png`.
- [x] T040 (spotter-pwa-dev) (040 AC-1–AC-5) `9a392a0` + `c000566` — join + console + PWA shell, 150-line vdom, `RoomClient` wrapper with pong-teed latency, SW that precaches the shell and never touches `/room/*`. 6 KB gz JS. Review approve with nits, nits fixed.
- [x] auditor `90a0e73` — eviction live test waits for the matching frame (pre-existing 1-in-5 flake).
- [x] T009 `b28cc2c` (020 AC-5 driver eviction); T017 `c9fb8b6` + `8bf5531` (client `onError`, `{code, terminal}`, terminal-close queue clear, `connect()` replay guard); docs `d633a5d`; `9fd612b`; `d447981`.
- [x] Earlier: T011 chain (`7bab61a`, `4181e34`, 020 AC-6); T006 chain (`68d9776`, `fa63af2`, `c1218e7`, 020 AC-8); T016 `0909521`; T008/T008a; T007a/T007c; T007/T007b; T004–T004c; T005/T005a — see `ralph/PROGRESS.md`.

## Notes / why

- 2026-09-04 design rounds: Maxx iterates live (phone + simulator) and each round is one lease with an Opus worker, one review, an in-lease nit pass, integrate, redeploy — ~30–40 min per round. Housekeeping task T054 collects the doc drift and small nits the reviews listed (see the review record) so they do not pile up: rewrite 030 R3/AC-4 + constitution §1 + BUILD_PLAN for the L/S strip; purge slider/throttle prose from the protocol + e2e skills, the spotter role and BUILD_PLAN; add `playwright-core` to `check:pins`; cap the auto-ack retry; `export function start(root)` in the spotter so wiring is testable; make every relay live test wait for a matching frame (flakes under parallelism).
- 2026-09-04 round 3: both consolidated app tasks landed in one iteration each with an in-lease nit pass — the consolidation paid for itself immediately. What is left before race day is mostly Maxx's: sideload (T101b), deploy (T102), first real-glasses run (T104), then T051 for whatever that shows. T050 (relay heartbeat + validation) is the only remaining pre-hardware code task and can run while Maxx tests.
- 2026-09-04 consolidation: Maxx judged the per-criterion slicing too expensive for an app this size. T020–T025 + 050's automated criteria → T030; spec 040 → T040; T012 + T013 (minus rate limiting) → T050; T014/T018/060/070 deferred. Consolidated tasks get an Opus-tier worker because a review block on a large task costs more than the model-tier difference.
- Image-first: the HUD is a bitmap composed from primitives; `hud-design.ts` is the single file to edit for a new look, and the golden snapshots pin whatever design is current. Text mode is ASCII-only, so the glyph-sheet hardware check is gone.
- The simulator (Maxx's machine, interactive window) is the functional check for the HUD; nibble order, pacing and `sendFailed` remain hardware questions, deliberately parked ("timers/update rates later").
- T030 and T040 are disjoint (glasses vs spotter) and run as parallel leases; T050 can run alongside either. The serial runner order is T030 → T040 → T050.
- `ralph/loop.sh` still honours only `owner:` and `[HW]`; `depends:`/`lock:` are honoured by agents reading the plan.
- The three untracked `.claude/agents/cavecrew-*.md` files are Maxx's; never delete, commit, or regenerate over them.
- AC audit 010: AC-1/2/6/7 met; AC-3/AC-5 `[SIM]`/T003c; AC-4 `[HW]`. 020: AC-1–6, 8 met; AC-7/AC-9 → T050; AC-10 `[HW]`. 030: AC-1–6 met, AC-7 `[SIM]` T026, AC-8/10 `[HW]`, AC-9 dropped. 040: AC-1–5 met, AC-6/7 `[HW]`. 050: AC-1–5 met, AC-5b `[SIM]` T026, AC-6–8 `[HW]`. 060/070 deferred.
- No criterion is `DISPUTED`; the 2026-09-04 spec changes (030 AC-9 dropped, 020 R4 rate limiting dropped, image-first) are human decisions recorded in each spec's Decisions. No simulator or hardware evidence is claimed; no deployment or secrets.
