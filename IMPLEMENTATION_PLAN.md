# Implementation plan — 2026-09-04T14:00:00-04:00 (consolidated)

Status: BUILDING
Current spec focus: specs/030 + specs/050 (glasses app, image-first) and specs/040 (spotter PWA)

## Model routing (either vendor per tier — pick per run, mixing is fine)

| Tier | Claude | OpenAI |
|---|---|---|
| worker — small task | Sonnet 5 @ high | GPT 5.6 Terra @ high |
| worker — consolidated task (T030/T040/T050 below) | Opus 5 @ high | GPT 5.6 Sol @ high |
| reviewer (`protocol-keeper` / `hud-qa`) | Opus 5 @ high | GPT 5.6 Sol @ high |
| planner (`plan-updater`) | Opus 5 @ xhigh | GPT 5.6 Sol @ xhigh |
| auditor (human-invoked final QA) | Fable 5.1 / Opus 5 @ xhigh | GPT 5.6 Sol @ xhigh |

## Consolidation (Maxx, 2026-09-04)

The one-criterion-per-iteration slicing cost ~3 iterations per task and the app is small. From here: **one task per app or subsystem**, a strong worker, one exact-range review per task, the auditor over the batch. Image HUD first (no text-first phase); text is an ASCII fallback. Rate limiting dropped; fake-spotter scenarios, client verifier polish (old T018), 060 hardening and 070 packaging deferred until after the first hardware session. Simulator runs are Maxx's (`npm run sim:scenarios` on his machine) once T030 lands.

## Active stream leases (interactive coordinator only)

- L017 — T030 — base `da6df0e` — write scope: `apps/glasses/**` (+ root `vitest.config.ts`/`package.json` only if discovery or a dev dependency needs it) — lock: `glasses-app` — worker: g2-glasses-dev (Opus 5 @ high) — reviewer: hud-qa (Opus 5 @ high) — auditor: Fable 5.1.
- L018 — T040 — base `da6df0e` — write scope: `apps/spotter/**`, `services/relay/wrangler.*` assets config only if hosting needs it — lock: `spotter-app` — worker: spotter-pwa-dev (Opus 5 @ high) — reviewer: hud-qa (Opus 5 @ high) — auditor: Fable 5.1.

## Next (ordered; the serial runner takes the first unchecked task)

- [ ] T030 (owner: g2-glasses-dev) (spec: 030 AC-1–AC-6 and R2–R5/R7/R8; 050 AC-1–AC-5 and R1–R3/R5/R6) [stream:glasses-app; lock:glasses-app; depends:T017 integrated (`c9fb8b6`)] **Glasses app end to end, image-first.** Build, in `apps/glasses/src/`: `render/primitives.ts` (pure drawing ops on a `Uint8Array(288*144)` of 0–15: `fillRect`, `fillTriangle`, `fillCircle`, `hline`, `vline`, `pixel`, `dim`), `render/hud-design.ts` (the ONLY layout file — symbol region + bar region per the `g2-hud-display` skill: ▲/●/▼ centred at x 144 in y 0–95, 20-cell bar outline level 6 / fill 15 with ticks at 72/144/216, `gap ≥ 90` swaps to outline 15 / fill 8, `linkOk:false` → every pixel `>> 1`), `render/draw-hud.ts` (`drawHud(state, {linkOk}) → Uint8Array`, pure), `render/gray4.ts` (`pack(frame) → Uint8Array(20736)`, high nibble = left pixel, row stride 144, plus `sdk-quirks.ts` for the `compressMode` shim in one place), `render/text.ts` (ASCII-only fallback: `^`/`o`/`v` or blank, 20-cell `#`/`-` bar + two spaces + integer gap, `!!` prefix at ≥ 90), `render/mode.ts` (`resolveRenderMode({search, kv})`: `?render=` → KV `g2rs:v1:render` → `'image'`; never reads `import.meta.env.MODE`), `render/queue.ts` (single async worker, one bridge call in flight, a new `hud` job replaces a pending one, gap-only changes debounced to `HUD_GAP_FLUSH_MS`, lane changes bypass, `msg` → `textContainerUpgrade` container 3, `status` → container 4, every call timed and logged `{call, ms, result}`, consecutive `sendFailed` counted → after 3 rebuild the page with a text container in slot 2 and stay there), `link.ts` (watchdog on `RoomClient.lastFrameAt` with an injected clock: `NO LINK` + dimmed HUD when `undefined` or older than `DRIVER_NO_LINK_MS`; recovers on any frame; status strings exactly `LINK OK · SPOTTER ON/OFF`, `NO LINK`, `CONNECTING…`, `ROOM ?`; `closed` with `terminal:true` shows the error and does not spin), `input.ts` (`toOsEvent` normaliser; `CLICK_EVENT` → `ack{msgId}` for the current unacked message, never clears locally; `DOUBLE_CLICK_EVENT` → `shutDownPageContainer(1)`; `FOREGROUND_ENTER_EVENT` → `connect(url)` debounced ≥ 1 s + re-render; exit events close the socket), `settings.ts` + companion page in `index.html` (room / PIN / name / render override via bridge KV `g2rs:v1:*`, log panel, Even App < 2.2.7 warning), and `main.ts` wiring (`waitForEvenAppBridge` → status `CONNECTING…` → ONE `createStartUpPageContainer` with the 4 containers from the skill (slot 2 = image, or text in text mode) → `RoomClient` with the browser `WebSocket` → store → queue). Render `msg.text` only while `ackedAt === null`. Import every type/constant from `@g2-race-spotter/protocol`. **Verifiers (all must run in root `npm test`):** `apps/glasses/test/draw-hud.test.ts` (ASCII golden snapshots 4× downsampled for `top/50`, `mid/62`, `bot/95`, `null/0`, dim variant = `>> 1`), `gray4.test.ts` (20 736 bytes; bright column x=0 → high nibble of byte 0 of every row), `render-text.test.ts` (exact strings incl. `!!`), `render-mode.test.ts` (URL > KV > image; simulator mode never changes it; exactly one startup call; text mode never creates an image container), `queue.test.ts` (10 gap states in 250 ms → ≤ 1 HUD send, last wins; lane bypasses; in-flight serialises), `link-watchdog.test.ts` (5 s silence → `NO LINK` + next HUD job dimmed; a `pong`-driven advance restores `LINK OK · …`), `ack.test.ts` (click sends ack once, text stays until `ackedAt` state, then hides; double click → `shutDownPageContainer(1)`), `fallback.test.ts` (3 × `sendFailed` → page rebuilt in text mode, later HUD updates use `textContainerUpgrade`). Mock the bridge behind a small `Bridge` interface so tests never import the SDK's runtime. Extra gates: every root gate, `npm run build -w apps/glasses`. Two commits allowed (render layer, then app layer); the reviewer reviews the whole lease range.
- [ ] T040 (owner: spotter-pwa-dev) (spec: 040 AC-1–AC-5; R1–R7) [stream:spotter-app; lock:spotter-app; depends:T017 integrated (`c9fb8b6`)] **Spotter PWA end to end** per `.agents/skills/spotter-ui/SKILL.md`: Join screen (room auto-uppercase 4–6, optional 4-digit PIN, name; persisted in `localStorage` `g2rs:v1:*`; relay host shown), Console (sticky status header with room / `DRIVER ONLINE|OFFLINE` / EWMA-of-5 ping latency / `✓ acked` or `… waiting`; header amber when driver offline, red `RECONNECTING` when the socket is closed with controls still enabled; lane stack ▲/●/▼ ≥ 88 px, selected reflects server state, tap on selected = no-op, "clear lane" → `lane:null`; car-behind slider ≥ 44 px hit height, `gap` at most every `GAP_SEND_MIN_MS` while dragging + once on release, chips 0·25·50·75·100, track colour toward red above 75; message input maxlength `MSG_MAX_CHARS`, Send/Clear, 5 recent chips deduped; landscape layout; safe-area padding; `navigator.vibrate?.(10)`), `RoomClient` wrapper using `onState`/`onConnection(state, {code, terminal})`/`onError` (terminal `auth` shows "wrong PIN" and returns to Join; `role_taken` not applicable to spotters), pure `view(state, conn) → vnode/string` for jsdom tests, `manifest.webmanifest` + service worker that never intercepts `/room/*` or `/health`, Vite build output where the Worker's assets config expects it. **Verifiers:** `apps/spotter/test/view.test.ts` (jsdom: AC-1 only ● selected + header text per `driverOnline`; AC-3 banner present and buttons enabled when closed, gone after replayed state; AC-4 ack tick when `ackedAt` non-null), `slider.test.ts` (AC-2: 30 values in 300 ms → ≤ 4 `gap` sends, last equals final), `bundle-size.test.ts` (AC-5: builds and gzips the main chunk, asserts ≤ 40 KB), plus a `recent-messages.test.ts` (dedupe, cap 5, localStorage round-trip). Add `jsdom` as a pinned devDependency (exact version, `check:pins`) if not present. Extra gates: every root gate, `npm run build -w apps/spotter`, `npm run check:pins`.
- [ ] T050 (owner: relay-backend-dev) (spec: 020 AC-7, AC-9; R4 minus rate limiting) [stream:relay-room; lock:relay-room; depends:T009 integrated (`b28cc2c`)] **Relay heartbeat + validation in one task.** `alarm()`: reduce `{t:'peer', online:false}` inline and broadcast *before* `close(4408)` (server close does not fire `webSocketClose`); add `attachment.rejected !== true` to `persistAndBroadcast`/`hasReadyPeer`; reap unready/roleless sockets that never sent `hello` (arm on accept or reap next tick — update the `alarm: null` assertions in `auth.test.ts` deliberately and say so); self-arm while sockets remain. Validation: `FRAME_MAX_BYTES` on UTF-8 byte length before `JSON.parse`; unknown `t` ignored and counted; malformed non-hello → `error{bad_frame}`, socket stays open; wrong-role frames ignored; `name=''` → absent; version precedence (`7bab61a`) preserved. **Verifiers:** `services/relay/test/heartbeat.test.ts` (spotter stops pinging → driver sees `spotterOnline === false` and spotter closes 4408 within `PEER_OFFLINE_MS + ALARM_TICK_MS`; an unready socket is reaped), `validation.test.ts` (AC-9). Extra gates: pinned live Wrangler tests, every root gate, `wrangler types --check`, isolated-assets dry-run.
- [ ] T051 (owner: g2-glasses-dev) (spec: 050 image polish) [depends:T030 integrated; T104/T109 evidence] Whatever the first hardware session shows: nibble order / stride flip, pacing, `sendFailed` behaviour, design tweaks in `hud-design.ts`. Scoped by Maxx after the session; do not start before.

## Needs simulator [SIM] — Maxx runs these on his machine

- [ ] T002c (owner: maxx) (spec: 010 AC-3) `npm run sim:scenarios -- --smoke` with simulator 0.9.5 open; commit `qa/<date>/sim/report.json` + `smoke-01.png` (records the real `getDeviceInfo()` for T003c).
- [ ] T026 (owner: maxx) (spec: 050 AC-5b, 030 AC-7) [depends:T030 integrated; needs a local relay: `npm run dev -w services/relay` + the spotter PWA or a hand-sent frame] `npm run sim:scenarios` in image mode for `lanes`, `gap-sweep`, `message-ack`, `link-loss`, `reconnect-replay`; commit `qa/<date>/sim/image/*.png` + `report.json`. This is the first look at the actual HUD design — expect to iterate `hud-design.ts` afterwards.

## Deferred (not before the first hardware session)

- T003c environment TBD removal (after T002c); T014 fake-spotter scenarios (real phones are the scenario runner); T018 client verifier polish (shared fakes, numeric-close detail, error-before-closed ordering, try/catch around listeners); spec 060 hardening (Android/iOS lifecycle, redeploy recovery, structured tail); spec 070 race-day packaging.

## Needs human [HW]

- [ ] T101b (owner: maxx) (spec: 010 AC-4) Enable Developer Mode, QR-sideload the glasses app, save the photo/log under `qa/<date>/`. **Do this right after T030 — it is the first real information the project gets.**
- [ ] T102 (owner: maxx) (spec: 020 AC-10) First Cloudflare deploy of the Worker (+ spotter assets after T040), set `DEBUG_KEY`, save `qa/<date>/020-deploy.log`.
- [ ] T104 (owner: maxx) (spec: 030 AC-8) Real-glasses `lanes`, `gap-sweep`, `message-ack`, `link-loss` from the spotter PWA; `qa/<date>/REPORT.md`.
- [ ] T106 (owner: maxx) (spec: 030 AC-10) `wss://` upgrade on hardware against the whitelisted origin; note in `docs/ENVIRONMENT.md`.
- [ ] T107 (owner: maxx) (spec: 040 AC-6) Install + join on iOS Safari and Android Chrome; landscape check; `qa/<date>/040-*.png`.
- [ ] T108 (owner: maxx) (spec: 040 AC-7) Two phones + glasses over LTE; lane-call latency in `qa/<date>/REPORT.md`.
- [ ] T109 (owner: maxx) (spec: 050 AC-6) Bright-column test pattern → nibble/stride confirmation in `docs/ENVIRONMENT.md`.
- [ ] T110 (owner: maxx) (spec: 050 AC-7) `gap-sweep` + 10-minute soak; `latency.csv`.
- [ ] T111 (owner: maxx) (spec: 050 AC-8) Exit dialogue → text fallback within 3 sends.
- Deferred with 060/070: T112–T117.

## Done this cycle

- [x] T009 `b28cc2c` (020 AC-5 driver eviction); T017 `c9fb8b6` + `8bf5531` (client `onError`, `{code, terminal}`, terminal-close queue clear, `connect()` replay guard); docs `d633a5d`; `9fd612b`; `d447981`.
- [x] Earlier: T011 chain (`7bab61a`, `4181e34`, 020 AC-6); T006 chain (`68d9776`, `fa63af2`, `c1218e7`, 020 AC-8); T016 `0909521`; T008/T008a; T007a/T007c; T007/T007b; T004–T004c; T005/T005a — see `ralph/PROGRESS.md`.

## Notes / why

- 2026-09-04 consolidation: Maxx judged the per-criterion slicing too expensive for an app this size. T020–T025 + 050's automated criteria → T030; spec 040 → T040; T012 + T013 (minus rate limiting) → T050; T014/T018/060/070 deferred. Consolidated tasks get an Opus-tier worker because a review block on a large task costs more than the model-tier difference.
- Image-first: the HUD is a bitmap composed from primitives; `hud-design.ts` is the single file to edit for a new look, and the golden snapshots pin whatever design is current. Text mode is ASCII-only, so the glyph-sheet hardware check is gone.
- The simulator (Maxx's machine, interactive window) is the functional check for the HUD; nibble order, pacing and `sendFailed` remain hardware questions, deliberately parked ("timers/update rates later").
- T030 and T040 are disjoint (glasses vs spotter) and run as parallel leases; T050 can run alongside either. The serial runner order is T030 → T040 → T050.
- `ralph/loop.sh` still honours only `owner:` and `[HW]`; `depends:`/`lock:` are honoured by agents reading the plan.
- The three untracked `.claude/agents/cavecrew-*.md` files are Maxx's; never delete, commit, or regenerate over them.
- AC audit 010: AC-1/2/6/7 met; AC-3/AC-5 `[SIM]`/T003c; AC-4 `[HW]`. 020: AC-1–6, 8 met; AC-7/AC-9 → T050; AC-10 `[HW]`. 030/050 automated → T030; 040 automated → T040. 060/070 deferred.
- No criterion is `DISPUTED`; the 2026-09-04 spec changes (030 AC-9 dropped, 020 R4 rate limiting dropped, image-first) are human decisions recorded in each spec's Decisions. No simulator or hardware evidence is claimed; no deployment or secrets.
