# Relay used by QR-sideloaded / dev builds (the page is served from the dev PC, not the relay).
# A page served by the relay itself ignores this. `?relay=` in the URL overrides both.
VITE_RELAY_URL=wss://g2-race-relay.maxx-384.workers.dev
